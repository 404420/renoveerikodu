// Existing Measurement ID found in the site's service/contact pages.
// Change measurementId here if the GA4 web stream uses a different ID.
// An empty ID disables analytics entirely; never put API secrets here.
window.RK_ANALYTICS_CONFIG = {
  measurementId: 'G-FHY5FDJ5DH',
  allowedHosts: ['renoveerikodu.ee', 'www.renoveerikodu.ee']
};
